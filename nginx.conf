events {}

http {
    upstream backend_servers {
        server server1:5000;
        # server server2:5000;
        # server server3:5000;
        # server server4:5000;
    }

    server {
        listen 5000;

        location / {
            proxy_pass http://backend_servers;
        }
    }
}